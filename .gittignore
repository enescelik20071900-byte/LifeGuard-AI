# --- Gizli Bilgiler ---
.env

# --- Veritabanları ---
*.db

# --- Python & Derleme ---
__pycache__/
*.pyc
*.pyo
*.pyd

# --- Sanal Ortamlar (Kütüphanelerin kurulu olduğu yer) ---
# Genelde venv veya env klasörleri buraya yüklenir, bunları ignore etmelisin
venv/
env/
.venv/

# --- IDE Dosyaları ---
.vscode/
.DS_Store

# --- Proje İçeriği (İgnore edilmemeli) ---
# mediapipe klasörünü buradan SİL, o bir kütüphanedir, ignore edilmez.
